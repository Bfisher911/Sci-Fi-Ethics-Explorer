'use client';

/**
 * Studio · Compare tab — multi-framework perspective comparison.
 * Pure wrapper around the existing <PerspectiveComparison /> component.
 */

import { PerspectiveComparison } from '@/components/analysis/perspective-comparison';

export function CompareTab() {
  return <PerspectiveComparison />;
}
